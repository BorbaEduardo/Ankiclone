import React from 'react';
import ReviewInterface from '@/components/Review/ReviewInterface'; // Adjust path as needed

const ReviewPage: React.FC = () => {
  // This page component might add layout, headers, footers etc. around ReviewInterface
  // For now, it just renders the main component.
  return <ReviewInterface />;
};

export default ReviewPage;

