import React from 'react';
import DeckDetail from '@/components/Deck/DeckDetail'; // Adjust path as needed

const DeckPage: React.FC = () => {
  // This page component might add layout, headers, footers etc. around DeckDetail
  // For now, it just renders the main component.
  return <DeckDetail />;
};

export default DeckPage;

